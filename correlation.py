def pearson_correlation(x,y):\\n    x_mean = sum(x) / len(x)\\n    y_mean = sum(y) / len(y)\\n    x_dev = [(xi - x_mean) for xi in x]\\n    y_dev = [(yi - y_mean) for yi in y]\\n    numerator = sum([x_dev[i]*y_dev[i] for i in range(len(x_dev))])\\n    denominator = (sum([(xi - x_mean)**2 for xi in x]) * sum([(yi - y_mean)**2 for yi in y]))**0.5\\n    return numerator / denominator