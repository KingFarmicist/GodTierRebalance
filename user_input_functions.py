import configparser\\ndef get_api_key():\\n    # Function to prompt user to enter their Coinbase or Binance API key\\n    # Returns API key input by user.\\n\\n    config = configparser.ConfigParser()\\n    config.read('config.ini')\\n    \\n    try:\\n        api_key = config['API Key']['key']\\n    except:\\n        api_key = input('Enter your Coinbase Pro or Binance API key: ')\\n        print('API key saved.')\\n        with open('config.ini', 'w') as configfile: \\n            config.set('API Key', 'key', api_key)\\n            config.write(configfile)\\n    \\n    return api_key\\n\\ndef get_user_preferences():\\n    # Function that prompts user to enter rebalancing thresholds, time intervals, and other custom parameters.\\n    # Returns dictionary of user preferences.\\n    \\n    print('Enter rebalancing thresholds and custom parameters')\\n    print('Value should be between 0 and 1.')\\n    print('Enter 0 if default value is to be maintained')\\n    thresholds = {}\\n    intervals = {}\\n\\n    for coin in ['BTC', 'ETH', 'MATIC', 'AVAX', 'SOL']:\\n        threshold = float(input(f'Enter threshold for {coin}: '))\\n        thresholds[coin] = threshold\\n    # Interval for which to perform rebalancing. Default value is 168 (weekly)\\n    rebalancing_interval = int(input('Enter rebalancing interval: '))\\n    \\n    preferences = {'thresholds': thresholds, 'rebalancing_interval': rebalancing_interval}\\n    # Save preferences to configuration file\\n    with open('config.ini', 'w') as configfile:\\n        config.set('Preferences', 'thresholds', str(thresholds))\\n        config.set('Preferences', 'rebalancing_interval', str(rebalancing_interval))\\n        config.write(configfile)\\n    \\n    return preferences